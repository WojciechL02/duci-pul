# Core ML frameworks
# Install PyTorch FIRST based on your GPU:
#   Blackwell (RTX 50 series): pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
#   Older GPUs (Ampere, etc): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then run: pip install -r requirements.txt

# Numerical and data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.2.0

# Image processing
pillow>=9.5.0
pycocotools>=2.0.6

# Hydra configuration
hydra-core==1.3.2
omegaconf>=2.3.0

# Deep learning utilities
einops>=0.6.0
tqdm>=4.65.0
timm>=0.9.0

# HuggingFace ecosystem
diffusers>=0.21.0
accelerate>=0.21.0
transformers>=4.30.0
huggingface-hub>=0.16.0
safetensors>=0.3.0
datasets>=2.14.0
openai

# Text processing (for CLIP)
ftfy>=6.1.0
regex>=2023.0.0

# ImageNet V2 dataset (install from GitHub)
git+https://github.com/modestyachts/ImageNetV2_pytorch

# Output formatting
tabulate>=0.9.0

# OpenAI CLIP (install from GitHub)
git+https://github.com/openai/CLIP.git

# Plotting (for analysis scripts)
matplotlib>=3.7.0
seaborn>=0.12.0
